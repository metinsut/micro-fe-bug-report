{
  "name": "micro-fe",
  "private": true,
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n remote,scheduler,host -c blue,magenta,green \"bun run dev:remote\" \"bun run dev:scheduler\" \"bun run dev:host\"",
    "dev:remote": "bun --filter remote dev",
    "dev:scheduler": "bun --filter scheduler dev",
    "dev:host": "bun --filter host dev",
    "build": "bun --filter remote build && bun --filter scheduler build && bun --filter host build",
    "test": "bun test"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
