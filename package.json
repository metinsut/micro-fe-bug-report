{
  "name": "micro-fe",
  "private": true,
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n remote,host -c blue,green \"bun run dev:remote\" \"bun run dev:host\"",
    "dev:remote": "bun --filter remote dev",
    "dev:host": "bun --filter host dev",
    "build": "bun --filter remote build && bun --filter host build",
    "test": "bun test"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
